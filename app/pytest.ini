[pytest]
asyncio_mode = auto
testpaths = tests
# Put the project root on sys.path so `from app import ...` works everywhere.
# The bare `pytest` command (unlike `python -m pytest`) does not add the CWD.
pythonpath = .
